<Window x:Class="CurrencyApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Currency App"
        Height="700"
        Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#1E1F22" />
        <SolidColorBrush x:Key="SidebarTextBrush" Color="#E8E8E8" />
        <SolidColorBrush x:Key="SidebarHoverBrush" Color="#2D2E32" />
        <SolidColorBrush x:Key="SidebarAccentBrush" Color="#4A9EFF" />

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{StaticResource SidebarHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0"
        Background="{StaticResource SidebarBackgroundBrush}">
            <StackPanel Margin="0,16,0,0">
                <TextBlock Text="Currency App"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource SidebarTextBrush}"
                           Margin="20,0,20,24" />

                <Button Content="Currencies"
                        Click="NavCurrencies_Click"
                        Style="{StaticResource NavButtonStyle}" />
                <Button Content="Add Currency"
                        Click="NavAdd_Click"
                        Style="{StaticResource NavButtonStyle}" />
                <Button Content="Settings"
                        Click="NavSettings_Click"
                        Style="{StaticResource NavButtonStyle}" />
            </StackPanel>
        </Border>

        <!-- Main content area -->
        <Frame Grid.Column="1"
               x:Name="RootFrame"
               NavigationUIVisibility="Hidden" />
    </Grid>
</Window>